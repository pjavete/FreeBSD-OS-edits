How to test our code:

1. Enter the command "sudo make backup"
2. Enter the command "sudo make move"
3. Compile the kernel
4. Return to directory and enter the command "make"
5. Enter the command "bash ./benchmark.sh"

To restore original files, enter the command "sudo make restore"
